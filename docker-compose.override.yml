version: '3.8'

services:
  runtime-launcher:
    image: nginx:alpine
    ports:
      - "8091:8091"
    volumes:
      - ./runtime-launcher.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
